#include "main.h"
/**
 *mul -Multiplies two integers
 *@x: The first to multiplies 
 *@y: The second number to multiply with first number
 *Return: The results f multiplication between x param and y param
*/
  it mul(int x,int x)
int Mul(int x,int y)
{
   
        return (0);
}
