#include “main.h”
/**
*  _strlen-returns the length of a string
* @s: string
*  return: length
*/
int  _strlen (char *s)
{
         int  longi= o;
         While (* s I = ‘\0’)
          {
              Longi ++;
              s++;
            }
  Return (longi);
}

